# Block direct browser access to config.php
<Files "config.php">
    Order Allow,Deny
    Deny from all
</Files>

# Force JSON responses and block direct PHP source viewing
Options -Indexes

# Allow CORS pre-flight
RewriteEngine On
RewriteCond %{REQUEST_METHOD} OPTIONS
RewriteRule .* - [R=204,L]
